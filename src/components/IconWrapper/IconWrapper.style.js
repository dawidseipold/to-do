import styled from 'styled-components';

export const StyledIconWrapper = styled.div`
  padding: 0.5rem;
  border-radius: 0.25rem;
  width: max-content;

  &:hover {
    cursor: pointer;
    background: #1f1f1f;
  }
`;
